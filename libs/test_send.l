;; (enter (load "./os.so")
;;        (system "rm -f /tmp/test-send")
;;        )

;; (enter (load "./socket.so")
;;        (setq s (socket AF_UNIX SOCK_DGRAM))
;;        (bind s "/tmp/test-send")
;;        (print s)
;;        (sendto s "Ping" "/tmp/test-recv")
;;        (setq mesg (recvfrom s 1024))
;;        (print mesg)
;;        (print (str (cdr mesg)))
;;        )

(enter (load "./socket.so")
       (setq s (socket AF_INET SOCK_DGRAM))
       (bind s '("127.0.0.1" . 0))
       (print s)
       (sendto s "Ping" '("127.0.0.1" . 8123))
       (setq mesg (recvfrom s 1024))
       (print mesg)
       (print (str (cdr mesg)))
       )

